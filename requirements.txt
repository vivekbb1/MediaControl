# Multi-Device Control Requirements

# Core device abstraction
# (no new dependencies needed)

# Apple TV control
pyatv>=0.14.0

# Android TV control uses 'adb' command-line tool
# Install Android SDK Platform Tools separately:
# - macOS: brew install android-platform-tools
# - Linux: sudo apt-get install android-tools-adb
# - Windows: Download from https://developer.android.com/studio/releases/platform-tools

# Optional: Pure Python ADB (alternative to command-line adb)
# adb-shell>=0.4.0
# pure-python-adb>=0.3.0

# Audio devices
soco>=0.30.0  # Sonos control
# Note: pyatv (already listed above) also handles AirPlay

# Existing requirements
broadlink>=0.19.0
requests>=2.31.0
pyyaml>=6.0
